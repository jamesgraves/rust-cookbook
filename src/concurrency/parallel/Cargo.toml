[package]
name = "parallel"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
glob = "0.3.0"
image = "0.24.5"
rand = "0.8.5"
rayon = "1.6.1"

[dev-dependencies]
cookbook_utils = { path = "../../cookbook_utils" }
