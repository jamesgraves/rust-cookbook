[package]
name = "dir-example"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
glob = "0.3.0"
same-file = "1.0.6"
walkdir = "2.3.2"

[dev-dependencies]
cookbook_utils = { path = "../../cookbook_utils" }
